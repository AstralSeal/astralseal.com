---
import { Image } from "astro:assets";
import coverImage from "~/assets/cafecover2.jpg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Image
  class="h-full w-full object-cover"
  src={coverImage}
  widths={widths}
  sizes={sizes}
  alt="The picture of cafe au lait"
/>
